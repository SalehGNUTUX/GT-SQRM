## [3.4.0] — 2026-07-14

### مقاطع خَلفيّة مُتَقَدّمة + Undo/Redo + عَلامة مائيّة مُطَوَّرة (منقولة من GT-SIRM v1.2)

#### مقاطع الخَلفيّة (playlist) — 12 تَحسيناً وإصلاحاً
- **👁️ إعماء (hidden) لِكُلّ مَقطع** — يَبقى في القائمة، يُتَخَطّى في التَبديل والـcrossfade والتَصدير. زَرّ 👁️/👁️‍🗨️ في كُلّ صَفّ. `getNextVisibleBgVidIdx`, `getFirstVisibleBgVidIdx`, `toggleBgVidHidden`.
- **✂️ تَقليم لِكُلّ مَقطع (per-clip trim)** — حَقلا "من/إلى" بجَنب كُلّ مَقطع + زَرّ ↺ إفراغ. يَعمَل في المُعاينَة والتَصدير (ffmpeg xfade+trim). `getBgClipTrimStart/End`, `hasBgClipTrim`, `setBgVidClipTrim`, `resetBgVidClipTrim`.
- **🎨 11 نَمط اِنتقال** — بَديل عن fade فَقط: `wipeleft/right/slideleft/right/up/down/circleopen/close/radial/dissolve`. اِنتقالات بـcanvas مَع softness (نُعومة حَواف gradient). عامّ عَبر `#bg-transition` أَو per-clip من dropdown في كُلّ صَفّ. `drawBgTransition`, `_makeClipPath`, `_getSoftMaskCanvas`, `BG_TRANSITION_TYPES`.
- **↩️ Undo/Redo عامّ** — 30 إجراءً مَحفوظاً، أزرار في `#bg-vid-toolbar`، اختصارات Ctrl+Z / Ctrl+Y / Ctrl+Shift+Z (تَجاهُل داخِل حُقول الإدخال). `S._history`, `historyPush`, `historyUndo`, `historyRedo`.
- **♻️ استعادة آخر مَقطع مَحذوف** — يَبحَث في history عن آخر `bgVidDelete`. `restoreLastDeletedBgVid`.
- **🌟 وميض ذَهبيّ عِند النَقل** — `.bgv-just-moved` animation + scroll إلى الصَفّ. `highlightMovedBgVidItem`.
- **🖱️ نَقر عَلى صَفّ يُنَشِّط المُعاينَة** — بَدون الحاجة لأَزرار.
- **🐛 Bug#4** — `switchToNextBgVid` يُوقِف المَقطع القَديم (بَعد التَحَقُّق مِن `hadCrossfade`) ويُعيد `currentTime = trimStart` عِند الحاجة. قَبل: كان القَديم يَستَمِرّ يَعمَل في الخَلفيّة.
- **✨ إصلاح الوَميض** — `updateBgVidCrossfade` تَنتَقِل فَوراً عِند `remaining <= 0` بَدَل اِنتظار `ended` مِن المُتَصَفِّح.
- **🔊 صَوت مُستَمِرّ** — `mixAudioToBuffer` تُبقي جميع المَقاطع المَرئيّة في timeline (buffer=null لِلصامِتة) بَدَل تَصفيتها. يُصلِح: صَوت مَقطع واحد كان يَستَمِرّ عَلى طول الفيديو المُصَدَّر.
- **📥 استعادة تَتابُعيّة** — `addBgVidItem` يُعيد Promise + خيار `silent`. الاستعادة تَستَخدِم `await` لِتَضمين الإعدادات (`hidden/trimStart/trimEnd/transition/audioEnabled/audioGain`) مُباشَرَةً + فَكّ صَوت المَقطع لَو مُفَعَّلاً.
- **🎬 WebM Infinity fix** — `getBgClipTrimEnd` يَستَخدِم `vid.duration` الحَيّة لَو كانت `item.dur=0` (WebM بمُدّة `Infinity`). `hasBgClipTrim` تَعمَل مَع WebM.
- **🟢 حاشية خَضراء** — `activateBgVidByIndex` يُنَبِّه `renderBgVidList` في النِهاية لِتَحديث المؤشِّر البَصريّ.

#### ffmpeg IPC (main.js)
- `extract-bg-frames` يَقبَل `clipTrims`, `transition`, `clipTransitions`.
- `xfadeAt(i-1)` — تَأثير الاِنتقال من المَقطع (i-1) إلى (i) يَستَخدِم إعداد المَقطع (i-1) إن حُدّد، وإلا العامّ.
- `XFADE_SAFE` — قائمة أَسماء xfade المَعروفة (يَحمي من قيمة مَجهولة تُفشِل ffmpeg).
- `filter_complex` يَستَخدِم `trim=start:end,setpts=PTS-STARTPTS` قَبل `scale/crop` لِكُلّ مَقطع.

#### العَلامة المائيّة
- `#wm-on` — توگل تَفعيل (اِفتراضيّاً مُفَعَّل، حِفاظاً عَلى السُلوك السابِق).
- `#wm-y-offset` — إزاحة رَأسيّة 0-40% (يَنطَبِق عَلى المَواضِع الأَربعة).

#### إصلاحات المُشَغِّل
- `playRecitationAudio` — يَبدأ من `S.elapsed` (يَدعَم استئناف مِن مَنتَصَف الآية).
- `pausePlayer` — يُوقِف `S.bgVidNext` أَيضاً (كان يَستَمِرّ يَعمَل في الخَلفيّة).
- `startPlayer` — يُشَغِّل `S.bgVidNext` أَيضاً لَو كان في مَنتَصَف الـcrossfade.
- `restartAll` — يُعيد `S.bgVid = bgVidItems[0].vid` + `bgVidNext = null` + `bgVidFadeProgress = 0` + إعادة تَعيين preview src.

#### إزالة سَلوك مُعطَّل
- `applyBgVidTrim` — أُزيل مُراقِب `timeupdate` البُغَيان (كان يَقفِل المَقطع في وَضع playlist بَدَل الاِنتقال). الآن `updateBgVidCrossfade` تُدير الحُدود.

## [3.3.6] — 2026-06-07

### إصلاح تداخل الصوت في وضع recvid (نفس v0.7.5 لـGT-SIRM)

1. startPlayer: إذا recvid-on مُفعَّل → pause bgAudioEl بدل play.
2. V2 export: إذا recvid → decode S.recVidFile كـ AudioBuffer وتمريره كـ bgBuffer
   بدل bgAudio القديم.

النتيجة: recvid هو مصدر الصوت الوحيد عند تفعيله.

## [3.3.5] — 2026-06-07

### إصلاح: removeBlackBackground is not defined

drawRecitationVideo المنقولة من GT-SIRM تستخدم removeBlackBackground (alias قديم
موجود في GT-SIRM فقط). أُضيفت الـ alias هنا أيضاً.

## [3.3.4] — 2026-06-07

### مزامنة دوالّ recvid مع GT-SIRM + إلغاء autoFitCanvasToVideo

#### المشكلة
- v3.3.3 أضافت autoFitCanvasToVideo التي تكسر FMT preset → معاينة وتصدير مشوَّهَين.
- اختلافات تنسيقيّة في 9 دوالّ recvid بين GT-SQRM و GT-SIRM.

#### الحلّ
- نُسخت 9 دوالّ بحرفيّة من GT-SIRM v0.7.3 إلى GT-SQRM:
  - drawRecitationVideo, onRecVidFile, removeRecVid
  - applyChromakeyToCanvas, removeBgColorFromRegion
  - getRecVidCanvas, getChromakeyCanvas, getLogoChromaCanvas, hexToRgb
- التطابق 100% الآن (diff = 0 سطر لكلّ دالّة).
- حُذفت autoFitCanvasToVideo نهائياً.
- الكانفاس يبقى عند الـ FMT المختار + فيديو التلاوة يُرسم بداخله مع fit/scale.

## [3.3.3] — 2026-06-07

### ضبط أبعاد الكانفاس على نسبة فيديو التلاوة

autoFitCanvasToVideo(vw, vh) في onRecVidFile.onloadedmetadata:
- إذا نسبة الفيديو ضمن 5% من preset → اختر الـ radio.
- وإلّا اضبط cv.width/height مباشرةً + fitCanvas().
- Toast بالحجم النهائيّ.

إصلاح: المعاينة كانت تأخذ حجم فيديو الخلفية بدل recvid.

## [3.3.2] — 2026-06-07

### ↺ زرّ إعادة من البداية في المشغّل

- زرّ `restart-all-btn` بجانب ⏮ ⏭ في شريط المشغّل (مطابق لـ GT-SIRM).
- يُعيد إلى البداية: الآية الحاليّة → 0، صوت القارئ، الصوت المخصّص، فيديو الخلفية،
  قائمة فيديوهات الخلفية (إلى المقطع الأوّل)، وفيديو التلاوة الجاهز.
- يحافظ على حالة التشغيل: إن كان playing → يستأنف بعد 60ms.

## [3.3.1] — 2026-06-07

### إصلاح تصدير recvid

حارس skipReciter جديد يمنع جلب صوت القارئ لشرائح recvid.
كان audioBuffers يجلب صوت آية 1 (~6s) ويستبدل manualDuration (90s)،
فيقصّ التصدير. الآن: ayaDurations[i] = manualDuration بدون fetch.
مُطبَّق في V1 + V2.

## [3.3.0] — 2026-06-07

### 🎥 تنظيم قسم التلاوة + إصلاحات التصدير

#### نقل فيديو التلاوة لأعلى تبويب التلاوة + إلغاء تلقائيّ لمصادر القرآن
- قسم 🎥 فيديو تلاوة جاهز أصبح في **أعلى** تبويب التلاوة.
- مصادر القرآن (السورة + القارئ + التوقيت) مغلّفة في `#quran-sources-wrap`.
- عند تفعيل `recvid-on`:
  - يُخفى الـwrap بالكامل تلقائياً.
  - `stopRecitationAudio()` يُوقف صوت القارئ.
  - `_recGen++` يُلغي أيّ جلب صوت جارٍ.
  - مسح verses القرآن لتجنّب التداخل في التصدير.
- عند إلغاء `recvid-on`:
  - يُظهر الـwrap.
  - يُعاد تحميل آيات السورة المختارة عبر `onSurahChange`.

#### إصلاح تصدير V2 الحتميّ (ffmpeg)
- `seekVideoToTime` ينتظر `seeked` event قبل كلّ `drawFrame(t)`.
- timeout أمان 800ms.

#### إصلاح تصدير V1 الحيّ (MediaRecorder)
- تشغيل recVidEl من 0 بعد بداية التصدير.
- إيقافه في `stopExportSources()`.

#### dirty tracking لرفع الملفّات
- `markProjectDirty()` في `onBgMedia`/`onBgAudio`/`addBgVidItem`/`onRecVidFile`.

## [3.2.0] — 2026-06-07

### 🎭 Chromakey + 🎥 فيديو تلاوة جاهز + 🖼️ شعار شفّاف

#### 🎭 Chromakey للوسائط (الخلفيّة)
- قسم جديد في "🎬 المشهد" قبل "🎞️ حركة الخلفية".
- color picker + presets (أخضر/أزرق).
- 3 ضوابط: التشابه (similarity), النعومة (smoothness), إزالة الانعكاس (spill).
- خوارزميّة YCbCr — دقّة لونيّة عالية + spill suppression.

#### 🎥 فيديو تلاوة جاهز
- قسم جديد في "📖 التلاوة" بعد قسم التوقيت.
- توگل `recvid-on` (افتراضي OFF) — لا يأخذ مساحة عند الإلغاء.
- ارفع فيديو (صوت + نصّ على خلفيّة لونيّة) → يستبدل القارئ.
- 4 أنماط ملاءمة (احتواء/تغطية/تمديد/الأصلي) + موضع X/Y + حجم.
- color picker لإزالة الخلفيّة (افتراضي أسود) + 4 presets.
- خوارزميّة موحّدة: أسود/أبيض → سطوع، ألوان → YCbCr.

#### 🖼️ Chromakey للشعار
- توگل في قسم "🖼️ شعار مخصص" (افتراضي OFF).
- يجعل لوناً محدّداً شفّافاً — للشعارات بدون قناة ألفا.
- color picker (افتراضي أبيض) + 3 presets + threshold + softness.

#### 📦 إضافات نظام المشاريع
- `S.recVidFile` يُضمَّن في `.gtsqrm` كأصل `recVideo`.
- استعادة فيديو التلاوة تلقائياً عند الفتح.

#### 🎨 إعادة ترتيب قسم النصوص
- نُقل "🎨 تنسيق النص" ليصبح **قبل** "🖋️ الخط" (مطابق لـ GT-SIRM).

## [3.1.0] — 2026-06-07

### 🎬 عنوان مخصّص للمقطع + 💾 نظام حفظ المشاريع

#### 🎬 عنوان المقطع (مستقلّ عن اسم السورة)
- قسم جديد في أعلى تبويب "النصوص": **🎬 عنوان المقطع**.
- حقل نصّ حرّ + موضع عمودي + حجم + لون + 4 تأثيرات (ظلّ/توهّج/حدود/بدون).
- يعمل جنباً إلى جنب مع اسم السورة (يمكن وضعهما عند ارتفاعَين مختلفَين).

#### 💾 نظام حفظ/فتح المشاريع (.gtsqrm)
- صيغة JSON (format=GT-SQRM-Project, formatVersion=1).
- أزرار في الرأس: 💾 المشروع + 📂 فتح.
- يحفظ: كلّ إعدادات DOM + المصادر (Base64 ≤50MB).
- File Association: النقر المزدوج على `.gtsqrm` يفتحه في GT-SQRM.
- `requestSingleInstanceLock` + `open-file` event.
- IPC: project-save-dialog/open-dialog/write/read.

#### ⏱️ الحفظ التلقائي
- قسم في تبويب الإعدادات: توگل + 1/5/10/15/30 دقيقة.
- ديسكتوب: حفظ صامت لآخر مسار + toast.

#### 🚪 حماية الإغلاق بـ modal ثلاثيّ
- اعتراض close في main → IPC → modal بأزرار: إلغاء/حفظ/إنهاء دون حفظ.
- علامة `●` في تاب المتصفّح + subtitle.

#### 🗂️ المصادر المفقودة عند الفتح
- نافذة modal تعرض كلّ مصدر مفقود مع input لاختيار بديل.

#### 🛠️ تخزين الملفّات الأصليّة
- `onBgMedia` و `onBgAudio` يحفظان الآن `S.bgImgFile` / `S.bgAudioFile` لإعادة التسلسل.

# سجل التغييرات | CHANGELOG

كل تغيير ملحوظ يُوثَّق هنا. التواريخ بصيغة YYYY-MM-DD.
الإصدارات تتبع [Semantic Versioning](https://semver.org/).

---

## [3.0.0] — 2026-05-17

إصدار رئيسي يجمع كل المزايا الواسعة المضافة بعد v2.0 ويوحّد التحزيم لثلاث صيغ Linux.

### 🚀 المزايا الكبرى الجديدة

#### قوالب منصات جاهزة
- اختيار سريع: Reels (1080×1920) / Shorts / TikTok / Square (1080×1080) / Landscape
- تطبيق الأبعاد + FPS + bitrate الأنسب لكل منصة بضغطة واحدة
- زر "حفظ كقالب" لإنشاء قوالب مستخدم مخزّنة في `localStorage` تحت `gt_sqrm_tpls`

#### اسم السورة في الأعلى (جديد كلياً)
- قسم مستقل في أعلى تبويب النصوص
- التحكم في: الموضع العمودي + حجم الخط + اللون + البادئة
- ثلاث صياغات: `سُورَةُ [الاسم]` / `[الاسم]` فقط / `حِزْبُ [الاسم]`
- تطبيع البادئة من اسم API لمنع التكرار

#### 10 أنماط ظهور للآيات + الوضع المختلط
- مباشر، تلاشي، انزلاق، تكبير، سقوط، صعود، ضبابي، دوران، ارتداد، يمين-يسار
- وضع **مختلط** يدوّر الأنماط بترتيب عشوائي ثابت حسب رقم الآية
- مزامنة كاملة بين "ظهور الآيات" في تبويب النصوص و"انتقالات الآيات" في تبويب التأثيرات

#### 9 أنماط ألوان متقدمة
- إعادة تسمية "color filter" إلى "أنماط الألوان"
- الأنماط: عادي، دافئ، بارد، ليلي، صحراء، سينمائي، أبيض-أسود، sepia، بنفسجي رمضاني
- تطبيق per-pixel ضمن خط الرسم الموحّد

#### 9 أشكال دبدبات صوتية + مضاعف الحدّة
- أشكال جديدة: bars / line / area / dots / mirror / radial / blocks / pulse / wave3D
- منزلق `wave-gain` يضاعف ارتفاع الموجة حتى 300%
- استخراج الطيف من البفر المُختلَط (FFT) في التصدير لضمان مطابقة المعاينة

#### خلفيات متعددة + crossfade
- قائمة تشغيل خلفيات فيديو، تبديل تلقائي، انتقال `xfade` ناعم بـ500ms
- إعادة الترتيب من السحب + التشغيل من الفهرس الجديد فوراً
- نفس التسلسل يُبنى كـ `filter_complex` في ffmpeg أثناء التصدير

#### 16 خطاً عربياً قرآنياً مضمّناً
- محلية بالكامل، تعمل دون اتصال
- زر "تحديث الخطوط" لاكتشاف الخطوط الجديدة المُضافة لمجلد `fonts/`
- 5 خطوط جديدة في v3: Amiri Quran, Reem Kufi, Scheherazade New, Aref Ruqaa, Markazi Text

#### كلمة-بكلمة + سرعة قابلة للضبط
- عرض الآية كلمة-بكلمة مع تلاشي لكل كلمة
- ثلاث سرعات: بطيء / متوسط / سريع
- مدة التلاشي قابلة للتعديل بـ منزلق `word-fade-ms`

#### تصدير V2 محسّن
- صوت per-clip للخلفية: تشغيل/كتم/مستوى مستقل لكل مقطع
- خيار كتم صوت المعاينة دون التأثير على التصدير
- خيار CRF (جودة) إضافة إلى bitrate
- اختيار الكوديك: h264 / h265 / vp9
- preset قابل للاختيار: ultrafast → veryslow

### 📦 التحزيم
- **هدف RPM جديد** — سكربت `npm run build:rpm` لتوليد `.rpm` لـ Fedora/RHEL/openSUSE
- **`npm run build:all`** — يبني AppImage + DEB + RPM في تشغيلة واحدة
- إضافة `maintainer` و `synopsis` لمعرّفات الحزم

### 🛠️ إصلاحات
- توحيد بادئة `localStorage` إلى `gt_sqrm_` (كانت `gt_sqr_tpls` في القوالب)
- إصلاح init order: `initEventListeners` قبل `restoreAllSettings`
- إصلاح FontFace dedup: التحميل غير مشروط حتى لـ `BUILT_IN_FONTS`
- إصلاح تكرار "سورة" حين تكون موجودة في اسم API
- إصلاح إغلاق `bgVid` المتقادم داخل حلقة V2

---

## [2.0.0] — 2026-05-16

### 🎯 الميزات الكبرى

#### محرّك تصدير V2 الحتمي
- **استبدال كامل** لخط `MediaRecorder` بمحرّك إطار-بإطار عبر `ffmpeg` مباشرة
- لا تقطّع، لا انجراف زمني — كل إطار يُرسم عند `t = i/FPS` بالضبط
- مدخل خام `rawvideo RGBA` بدل JPEG → ~5× أسرع
- اِستخراج إطارات فيديو الخلفية مسبقاً كـ JPEG (يحلّ مشكلة "اختفاء الفيديو")
- مزامنة صوتية كاملة عبر `OfflineAudioContext` → WAV → ffmpeg
- إصلاح مشكلة مدّة MP4 المعروفة (لا حاجة لـ `fixWebmDuration`)
- إعداد افتراضي `preset=veryfast` لتسريع الترميز 3-4×

#### بحث الآيات والسور بتطبيع التشكيل
- حقل بحث في تبويب التلاوة يبحث في كامل القرآن (6,236 آية)
- تطبيع كامل: إزالة التشكيل + توحيد الألف (`أإآ→ا`) + `ى→ي` + `ة→ه` + إزالة التطويل
- "الفاتحه" يطابق "الفَاتِحَة"، "الرحمن" يطابق "الرَّحْمَٰن"
- تظليل النص المُطابِق بـ `<mark>` ذهبي مع خريطة مواقع تربط المُطبَّع بالأصلي
- نقر النتيجة → انتقال فوري للسورة والآية
- بحث منفصل لأسماء السور بنفس نهج التطبيع
- Debounce 200ms + Esc للمسح

#### القرآن كاملاً محلياً (دون اتصال)
- تحميل تلقائي للقرآن العثماني الكامل في خلفية أوّل تشغيل
- تخزين دائم في `localStorage` (`gt_sqrm_quran_idx_v1`، ~1.5MB)
- `loadVerses` تستعمل الفهرس قبل اللجوء لـ API
- ترجمات بكاش لكل (سورة، نسخة): `gt_sqrm_trans_{ed}_{n}`
- مؤشّر المصدر في `aya-info`: 📚 محلي / 🗂 جلسة / 🌐 شبكة
- قائمة السور في `localStorage` بدل `sessionStorage` (دائمة)

#### عرض كلمة-بكلمة للآيات
- خيار رابع لـ `tanim` يكشف كلمات الآية تباعاً بترتيبها الأصلي
- 4 إيقاعات: تلقائي (يُوزّع على مدة الآية) / 0.35 / 0.5 / 0.75 / 1.0 ث/كلمة
- خيار إبقاء الكلمات السابقة أو إخفاؤها
- مدّة تلاشي ظهور قابلة للضبط (0-500ms)
- يحافظ على تخطيط `wrapText` الكامل (لا قفز في المواقع)

#### رفع متعدد لخلفيات الفيديو + ترتيب
- رفع عدة مقاطع دفعة واحدة عبر `multiple` input
- قائمة قابلة لإعادة الترتيب (▲ ▼ ✕)
- تتابع تلقائي بين المقاطع (cycle عند نهاية الأخير)
- **انتقال crossfade 500ms سلس** بين المقاطع في المعاينة
- **xfade filter في تصدير V2** عبر `ffmpeg filter_complex` (مطابق للمعاينة)
- تأثير فوري عند إعادة الترتيب (يُشغَّل المقطع الأول من الترتيب الجديد فوراً)

#### تقطيع زمني للوسائط المحلية
- ✂️ توجل + حقلَي بداية ونهاية تحت كل من:
  - فيديو الخلفية → يكرّر هذا الجزء فقط في المعاينة والتصدير
  - صوت الخلفية → نفس المنطق
- في المعاينة: `timeupdate` يُعيد للبداية عند تجاوز `end`
- في تصدير V2: `-ss/-to` لاستخراج إطارات + `sliceAudioBuffer` للصوت

#### قوالب جاهزة للمنصات
- منتقي في تبويب الإعدادات يضبط الأبعاد + FPS + الجودة بنقرة:
  - Reels/Shorts/TikTok 1080×1920 — 30fps · 10 Mbps · CRF 20
  - Reels 720×1280 سريع — 30fps · 6 Mbps
  - YouTube 1920×1080 — 30fps · 12 Mbps
  - YouTube 1280×720 — 30fps · 8 Mbps
  - Instagram Square 1080×1080 — 30fps · 8 Mbps
  - Instagram Portrait 1080×1350 — 30fps · 8 Mbps
  - Cinema 21:9 2560×1080 — 30fps · 15 Mbps
- إضافة نسبة 4:5 لراديوهات `fmt` الموجودة

#### 16 خطاً عربياً قرآنياً
ملفات FontFace فعلية محمَّلة محلياً، بما فيها:
- **خطوط قرآنية حرة جديدة (SIL/Google OFL)**:
  - Scheherazade New (SIL)
  - Lateef (SIL)
  - Harmattan (SIL)
  - Reem Kufi (Google)
  - Tajawal (Google)
- **خطوط أضافها المستخدم**:
  - خط نسخ العثماني
  - AALMaghribi
- خطوط أصلية محفوظة (Amiri Quran, Uthmanic Hafs, Noto Sans Arabic إلخ)

### 🛠️ إصلاحات حرجة
- **تحميل ملفات FontFace**: `loadLocalFonts` كانت تتخطّى تحميل الملف إذا الاسم في `BUILT_IN_FONTS` → خطوط ظاهرة بالاسم لكن لا تُرسَم. الآن يُحمَّل الملف **دائماً**
- **اختفاء فيديو الخلفية في V2**: `fetch(blob:...)` محظور تحت CSP → استخدام `File.arrayBuffer()` مباشرة، مع إضافة `blob:` و `data:` لـ CSP كاحتياط
- **bug ترتيب init**: `restoreAllSettings()` كانت تُستدعى قبل `initEventListeners()` → أحداث `change` تُبثّ على مستمعين غير مسجَّلين → لوحات `bg-vid-ctrl` تبقى مخفية حتى يُبدّل المستخدم يدوياً. الآن الترتيب: مستمعون → استعادة
- **التأثير الفوري عند إعادة ترتيب المقاطع**: كان ينتظر انتهاء الحالي. الآن يُشغَّل المقطع الأول من الترتيب الجديد فوراً
- **bgVidItems لا يُشغَّل تلقائياً**: إضافة `vid.play()` بعد التحميل
- **mismatch IPC**: `writeTempFile` غير موجود، تمت إضافته كـ alias لـ `writeTempBuffer`

### 🎨 تحسينات UX
- إعادة تسمية "المكساج" → **"المزج"**
- لون مستقل للإطار الذهبي (`gold-col`) — كان يستخدم `orn-col`
- حفظ الشعار المخصّص في `localStorage` (`gt_sqrm_logo_v1` — الصور فقط، الفيديوهات أكبر من السعة)
- ذاكرة مجلد التصدير الأخير (`gt_sqrm_lastExportDir`) — معزولة عن مسار yt-dlp
- 8 تأثيرات بصرية جديدة: Pixelate · Mosaic · Ripple · Wave · Swirl · Kaleidoscope · Glitch · Old Film
- شريط تقدّم محسَّن أثناء تصدير V2 مع تفاصيل الإطار والزمن
- ftpeg preset افتراضي `veryfast` للسرعة (يمكن تغييره)

### ⚙️ تحسينات داخلية
- خنق رسائل `ffmpeg-progress` IPC إلى 250ms لتفادي فيضان
- تنظيف temp files تلقائياً عند الإلغاء/الفشل
- `read-tmp-file` IPC آمن (يقيّد القراءة على `os.tmpdir()` فقط)
- استخدام `data.buffer` مباشرة بدون نسخة إضافية في raw RGBA pipe

### 📊 الأرقام
| المقياس | قبل | بعد |
|---|---|---|
| مدّة تصدير 34 ثانية | 22-44 دقيقة (متقطّع) | 1-2 دقيقة (سلس) |
| إطار/ثانية في الحلقة | ~1-2s/إطار | ~50-80ms/إطار |
| الخطوط الفعّالة | 8 | 16 |
| دعم العمل دون اتصال | 7 آيات الفاتحة | 6,236 آية |

---

## [1.1.0] — 2026-05-09 (قبل الجلسة الحالية)

### ✅ مُنجَز سابقاً
- إصلاح مجلد الحفظ الدائم لـ yt-dlp
- فصل كتم المعاينة عن كتم التصدير
- قائمة سياقية محسّنة (نسخ/لصق)
- إعادة ترتيب أزرار التالي/السابق (RTL)
- أيقونة "حول" رسمية بدل الإيموجي

---

## [1.0.0] — الإصدار الأولي

- إنشاء النسخة المكتبية بـ Electron انطلاقاً من GT-SQR (النسخة الويب)
- دمج ffmpeg + yt-dlp
- تصدير بـ MP4/WebM/MKV
- 30+ قارئ
- 8 سمات لونية
- 6 زخارف إسلامية
